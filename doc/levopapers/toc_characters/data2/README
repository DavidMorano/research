September 5 about 11:28


This was the first data for memory (Thursday morning Sep. 5).
This did not include lifetime-close-out for the memory (but it
did for the registers -- which we want to ignore anyway for
errors due to missing instructions).

There is only the 64K long 2^0 density tables for the memory
stuff.



