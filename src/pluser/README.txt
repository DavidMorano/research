PLUSER

This program adds a tab character to the beginning of each line
that starts with the two-character sequence '+ '.

Synopsis:
$ plauer < <file>

Arguemtns:
none		buf the program expects input on STDIN

Returns:
Writes the output file to STDOUT.

